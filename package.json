{
  "name": "cinepass",
  "version": "2.0.0",
  "description": "Galaxy Studio — Cinema booking system (React + NodeJS)",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install:all": "cd server && npm install && cd ../client && npm install",
    "migrate": "cd server && node src/scripts/migrate-passwords.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
